---
interface Props {
  number: string;
  text: string;
}
const { number, text } = Astro.props;
---

<span class="inline-block text-[0.7rem] font-semibold tracking-[0.35em] uppercase text-gold mb-4">
  {number} — {text}
</span>
