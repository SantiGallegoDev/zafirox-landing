<section class="relative py-20 lg:py-24 overflow-hidden">
  <div class="section-divider"></div>

  <!-- Background glow -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[700px] h-[450px] bg-[radial-gradient(ellipse,rgba(255,255,255,0.03)_0%,transparent_70%)]"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[350px] bg-[radial-gradient(ellipse,rgba(198,168,94,0.06)_0%,transparent_70%)]"></div>
  </div>

  <div class="max-w-3xl mx-auto px-5 relative z-10">
    <div class="glass-card rounded-2xl p-8 sm:p-12 text-center fade-in">
      <!-- Gift / Free trial icon -->
      <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-gold/10 border border-gold/20 mb-6">
        <svg class="w-8 h-8 text-gold" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"/>
        </svg>
      </div>

      <span class="inline-block text-[0.65rem] font-bold tracking-[0.3em] uppercase text-gold border border-gold/30 px-4 py-1.5 rounded-full bg-gold/5 mb-5">
        7 días gratis
      </span>

      <h2 class="text-[clamp(1.4rem,3vw,2rem)] font-bold mb-3">
        Pruébalo sin riesgo,<br class="hidden sm:block" />
        <span class="text-gold">paga solo si te convence</span>
      </h2>
      <p class="text-gray-300 text-sm mb-8 max-w-lg mx-auto leading-relaxed">
        Acceso completo durante una semana. Sin tarjeta de crédito, sin compromisos, sin letras chicas.
      </p>

      <!-- Guarantee points -->
      <div class="grid sm:grid-cols-3 gap-6 text-left">
        <div class="flex gap-3">
          <svg class="w-5 h-5 text-gold shrink-0 mt-0.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
          </svg>
          <div>
            <p class="text-bone text-sm font-semibold">Acceso completo</p>
            <p class="text-gray-400 text-xs mt-1">Todas las funciones del plan Profesional durante 7 días.</p>
          </div>
        </div>
        <div class="flex gap-3">
          <svg class="w-5 h-5 text-gold shrink-0 mt-0.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
          </svg>
          <div>
            <p class="text-bone text-sm font-semibold">Setup gratuito</p>
            <p class="text-gray-400 text-xs mt-1">Nosotros te configuramos todo. Tú solo llegas a operar.</p>
          </div>
        </div>
        <div class="flex gap-3">
          <svg class="w-5 h-5 text-gold shrink-0 mt-0.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
          </svg>
          <div>
            <p class="text-bone text-sm font-semibold">Soporte humano</p>
            <p class="text-gray-400 text-xs mt-1">Soporte real por WhatsApp. Nada de bots ni tickets.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
